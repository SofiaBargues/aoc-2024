{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "launch",
      "name": "Debug Puzzle",
      "program": "${file}",
      "args": ["${relativeFileDirname}/${fileBasenameNoExtension}.data.txt"],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "watchMode": false
    },
    {
      "type": "bun",
      "request": "launch",
      "name": "Debug Puzzle (Sample)",
      "program": "${file}",
      "args": [
        "${relativeFileDirname}/${fileBasenameNoExtension}.sample-data.txt"
      ],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "watchMode": false
    }
  ]
}
